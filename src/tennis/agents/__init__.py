"""Agent implementations. Each subpackage = one agent in the 4-agent pipeline.

Currently only `research.validator` is implemented — it's the contract gate
between the (future) Research and Modeling agents and is intentionally NOT
inside either agent. The full Research and Modeling agents arrive in later
days.
"""
